<!DOCTYPE html>
<html>
<head>
  <title>HackOverflow</title>
  <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default">
    <ul>
      <li><%= link_to 'Home', root_path %></li>
      <% if is_authenticated? %>
        <li>Logged in as: <%= current_user.username%></li>
        <li class = "move-right"><%= link_to 'Logout', logout_path, method: 'delete' %></li>
        <li><%= link_to 'Edit Account', edit_user_path(current_user.id)%></li>
      <%else%>
        <li class = "move-right"><%= link_to 'Sign-Up', new_user_path %></li>
      <%end%>
  </ul>
</nav>

  <h1><img id ="icon" src = "http://sstatic.net/stackoverflow/img/apple-touch-icon.png"/>Hack<span>OverFlow</span></h1>

<%= yield %>

</body>
</html>
